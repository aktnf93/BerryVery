<Window x:Class="BerrDeviceSimulator.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BerrDeviceSimulator.Views"
        xmlns:vm="clr-namespace:BerrDeviceSimulator.ViewModels"
        mc:Ignorable="d"
        Title="Device Simulator" Height="450" Width="800"
        Loaded="{local:MethodBinding OnLoaded}"
        >
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 메인 제어 패널 -->
        <Grid Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0">
            <Border Background="LightGray"/>
        </Grid>

        <!-- 필터링 패널 -->
        
        
        
        <!-- 디바이스 목록 -->
        <Grid Grid.Column="1" Grid.Row="1">
            <Border Background="LightBlue"/>

            <DataGrid ItemsSource="{Binding DeviceMonitoringView}"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserResizeRows="False"
                      AutoGenerateColumns="False"
                      >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Port" Binding="{Binding Port.PortAddress}"/>

                </DataGrid.Columns>
            </DataGrid>
        </Grid>

    </Grid>
</Window>
